apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-creator-service
  namespace: ns-appointment-creator-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appointment-creator-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: appointment-creator-service
    spec:
      serviceAccountName: sa-appointment-creator-service
      containers:
        - name: appointment-creator-service
          image: jsfelipearaujo/appointment-creator-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 4
            successThreshold: 1
          resources:
            limits:
              memory: 256Mi
              cpu: 100m
            requests:
              memory: 128Mi
              cpu: 100m
          envFrom:
            - configMapRef:
                name: appointment-creator-service-config
      terminationGracePeriodSeconds: 30
      nodeSelector: {}
      tolerations: []
